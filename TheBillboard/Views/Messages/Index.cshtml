@model IEnumerable<Message>

@{
    ViewData["Title"] = "Messages";
}

<div class="text-center">
    <h1 class="display-4">@ViewData["Title"]</h1>
    
    <ul class="list-group list-group-flush align-items-center">
        @foreach (var message in Model)
        {
            <li class="list-group-item">
                <div class="card">
                    <div class="d-flex flex-column card-body">
                        <h5 class="card-title">@message.Title</h5>
                        <h6 class="card-subtitle mb-2 text-muted">@message.Author</h6>
                        <p class="card-text">@message.Body</p>
                        @Html.ActionLink("Detail", "Detail", new { id = message.Id }, new { @class = "btn btn-sm btn-outline-primary w-25" })
                        @Html.ActionLink("Edit", "Create", new { id = message.Id }, new { @class = "btn btn-sm btn-outline-danger w-25" })
                    </div>
                    <div class="d-flex flex-column align-items-end card-footer">
                        <small class="text-muted"><strong>Updated at: </strong> @message.FormattedUpdatedAt</small>
                    </div>
                </div>
            </li>
        }
    </ul>
</div>